//import { tns } from "./node_modules/tiny-slider/src/tiny-slider.js";
console.log("Hello from bn slider");
/*var breakingNewsSlider = tns({
    container: ".breaking-news-slider",
    items: 1,
    slideBy: "page",
    autoplay: true,
    autoplayTimeout: 4000
});

addLoadEventCallback( () => {
    let slider = document.querySelector(".breaking-news-slider");
    
    slider.addEventListener("mouseenter", () => {
        console.log("Mouse in");
        let sliderStop = document.querySelector(".breaking-news-section button[data-action='stop']");
        sliderStop.click();
    });
    slider.addEventListener("mouseleave", () => {
        console.log("Mouse out");
        let sliderStart = document.querySelector(".breaking-news-section button[data-action='start']");
        sliderStart.click();
    });
});*/